"""
Exercise 1: Variables and data types

Task:
Create four variables describing yourself (or a made-up person, if you'd
rather not use real details):

    name        (a str)
    age         (an int)
    height_m    (a float, your height in meters, e.g. 1.75)
    is_learning_to_code   (a bool, should be True)

Then, using a single f-string, print one sentence that includes all four
values, similar in shape to:

    "Ada is 28 years old, 1.75m tall, and learning to code: True"

(Your exact wording can differ. The point is combining all four variables
into one printed sentence using an f-string, not matching this text exactly.)

Expected behavior:
Running this file should print one sentence containing all four values.
"""

# your code here

"""
Exercise 7: Functions (parameters and return)

Task:
Write a function called `build_greeting` that takes two parameters,
`name` and `time_of_day` (e.g. "morning", "afternoon", "evening"), and
RETURNS (not prints) a greeting string like:

    "Good morning, Ada!"

Then, below your function definition, call it a couple of times with
different arguments, store each result in a variable, and print those
variables.

This is deliberately testing the print-vs-return distinction from the
notes: build_greeting should NOT print anything itself. All the printing
happens outside the function, using the value it returned.

Expected behavior:
Calling build_greeting("Ada", "morning") should produce the string
"Good morning, Ada!" (as a return value, which you then print yourself).
"""

# your code here (define build_greeting)


# your code here (call it at least twice and print each result)

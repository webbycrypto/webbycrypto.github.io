"""
Exercise 6: Loops (while, and the accumulator pattern)

Task, part 1:
Using a `while` loop (not a `for` loop), print a countdown from `start`
down to 1, one number per line, then print "Liftoff!" after the loop
ends. Be careful: this is exactly the kind of loop where forgetting to
update your counter variable inside the loop causes it to run forever.
If you run this and it seems stuck printing the same number over and
over, stop it (Ctrl+C in the terminal) and check that you're actually
decreasing your counter each pass.

Task, part 2:
Using a `while` loop again, calculate the sum of every whole number from
1 up to and including `start`, using the accumulator pattern from the
notes (a `total` variable that starts at 0 before the loop, and gets
added to on each pass). Print the final total.

Expected behavior:
For start = 5, part 1 should print 5, 4, 3, 2, 1, then "Liftoff!".
Part 2 should print 15 (1 + 2 + 3 + 4 + 5).
"""

start = 5

# your code here (part 1: countdown)


# your code here (part 2: running total)
